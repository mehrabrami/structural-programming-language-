#include <stdio.h>

int main() {
    // Declaring i and   initialising 
    int i=1;
    // using while loop
    while (i <= 5) {
        printf("%d ", i);
        i++; // increment
    }
    return 0;
}
