#include <stdio.h>

int main() {
    int studentID = 2025331539;
    float score = 3.45;
    char grade = 'A';

    printf("StudentId is %d", studentID);
    printf("\nScore is %.2f", score);
    printf("\ngrade is %c", grade);
    return 0;
}
